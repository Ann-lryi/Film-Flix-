name: Android CI — Build Debug APK

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:   # Cho phép chạy tay từ tab Actions

# Hủy run cũ khi push commit mới lên cùng nhánh — tiết kiệm phút CI
concurrency:
  group: android-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build APK Debug
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup JDK 17 (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Kiểm tra môi trường & Cấp quyền thực thi Gradlew
        run: |
          java -version
          echo "SDK: $ANDROID_HOME"
          chmod +x ./gradlew
          ls -l ./gradlew

      - name: Build APK Debug
        run: chmod +x ./gradlew && ./gradlew assembleDebug --stacktrace --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: NguonCStream-debug-apk
          path: app/build/outputs/apk/debug/*.apk
          retention-days: 30
          if-no-files-found: error
